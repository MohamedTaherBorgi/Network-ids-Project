What this file does

✔ Loads ML model
✔ Converts your packet feature dict into a numeric vector
✔ Supports:

decision_function (OneClassSVM, IsolationForest, etc.)

predict_proba (probabilistic models)

predict (fallback)

✔ Returns:

(True, score)   → anomaly detected
(False, score)  → normal traffic
